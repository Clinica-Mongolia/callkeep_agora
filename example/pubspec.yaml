name: example
description: Flutter project for Agora video call

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  callkeep:
    path: ../
  firebase_core: ^1.3.0
  firebase_messaging: ^10.0.2
  flutter_local_notifications: ^6.0.0
  uuid: ^3.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
